extends layouts/main.pug
block head_scripts
  link(rel="preload" href="/css/404.css?ver={{cache_version}}" as="style" onload="this.rel='stylesheet'")
block body
  #logoSVG
    img(src="/images/logo.svg" alt="Logo-Agometra")
  h1 404 - Страница не найдена!
  a(href='/') Перейти на главную
